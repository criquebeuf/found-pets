<h1>Found pets!</h1>

<% @pets.each do |pet| %>
    <p><%= link_to pet["name"].capitalize, pet_path(pet["id"])%><%= pet.address%> <%= link_to '🗑️', pet_path(pet), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></p>
<% end %>

<%= link_to "+Pet", pets_new_path %>
